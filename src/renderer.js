// takes the template and data then pumps it through the liquid rendereing engine
// then replaces the content of the selected element with the rendered results
// when it's done it fires the progressEnd event